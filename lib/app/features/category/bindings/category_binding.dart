import 'package:get/get.dart';

class CategoryBinding extends Bindings {
  @override
  void dependencies() {
    // A dependência do CategoryController foi movida para o InitialBinding
    // para estar disponível globalmente.
  }
}
